<%- include('partials/head') %>
<%- include('partials/header') %>

<div class="contacts-container">
  <% if (user) { %>
    <div class="alert alert-success text-center mb-4">
      ¡Bienvenido, <%= user.email %>! (Rol: <%= user.role %>)
    </div>
  <% } %>

  <h2 class="mb-4">📇 Contactos</h2>

  <form method="POST" action="/contacts" class="row g-3 mb-5 p-3 bg-white shadow-sm rounded">
    <div class="col-md-2">
      <input type="number" name="company_id" class="form-control" placeholder="Company ID" required />
    </div>
    <div class="col-md-2">
      <input type="text" name="first_name" class="form-control" placeholder="First Name" required />
    </div>
    <div class="col-md-2">
      <input type="text" name="last_name" class="form-control" placeholder="Last Name" />
    </div>
    <div class="col-md-2">
      <input type="text" name="phone" class="form-control" placeholder="Phone" />
    </div>
    <div class="col-md-2">
      <input type="email" name="email" class="form-control" placeholder="Email" />
    </div>
    <div class="col-md-2">
      <input type="text" name="title" class="form-control" placeholder="Title" />
    </div>
    <div class="col-md-6">
      <input type="url" name="linkedin" class="form-control" placeholder="LinkedIn URL" />
    </div>
    <div class="col-md-12 text-end">
      <button type="submit" class="btn btn-primary px-4">➕ Guardar Contacto</button>
    </div>
  </form>

  <div class="table-responsive">
    <table class="table contacts-table">
      <thead class="table-light">
        <tr>
          <th>Nombre</th>
          <th>Teléfono</th>
          <th>Email</th>
          <th>Cargo</th>
          <th>LinkedIn</th>
        </tr>
      </thead>
      <tbody>
        <% contacts.forEach(c => { %>
          <tr>
            <td data-label="Nombre"><%= c.first_name %> <%= c.last_name %></td>
            <td data-label="Teléfono"><%= c.phone %></td>
            <td data-label="Email"><%= c.email %></td>
            <td data-label="Cargo"><%= c.title %></td>
            <td data-label="LinkedIn">
              <% if (c.linkedin) { %>
                <a href="<%= c.linkedin %>" target="_blank" class="btn btn-sm btn-outline-primary">Ver Perfil</a>
              <% } else { %>
                <span class="text-muted">N/A</span>
              <% } %>
            </td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  </div>
</div>

<%- include('partials/footer') %>
