<%- include('partials/head') %> 
<%- include('partials/header') %>

<!-- 
  🌐 organizations.ejs

  📌 Vista que muestra todas las empresas registradas en el sistema CRM.

  ✅ Muestra:
    - Nombre (como link a detalles)
    - Teléfono
    - Website
    - Dirección (calle + ciudad + estado)
    - Account Manager

  ✅ Responsive y limpio, con subtítulo y tabla adaptativa.
-->

<div class="container mt-4">
  <h2 class="mb-1">Companies</h2>
  <h3 class="contact-subtitle">Organization Directory</h3>

  <!-- 📦 Tabla de organizaciones -->
  <div class="table-responsive">
    <table class="table table-hover shadow-sm bg-white rounded">
      <thead class="table-light">
        <tr>
          <th>Nombre</th>
          <th>Teléfono</th>
          <th>Website</th>
          <th>Dirección</th>
          <th>Account Manager</th>
        </tr>
      </thead>
      <tbody>
        <% organizations.forEach(org => { %>
          <tr>
            <!-- 🔗 Nombre como link a la vista de detalle -->
            <td data-label="Nombre">
              <a href="/organizations/<%= org.id %>" class="text-decoration-none fw-semibold">
                <%= org.name %>
              </a>
            </td>

            <!-- ☎️ Teléfono -->
            <td data-label="Teléfono"><%= org.phone %></td>

            <!-- 🌐 Website con enlace -->
            <td data-label="Website">
              <% if (org.website) { %>
                <a href="<%= org.website %>" target="_blank" class="text-decoration-none"><%= org.website %></a>
              <% } else { %>
                <span class="text-muted">N/A</span>
              <% } %>
            </td>

            <!-- 🏠 Dirección -->
            <td data-label="Dirección">
              <%= org.billing_street %>, <%= org.billing_city %>, <%= org.billing_state_province %>
            </td>

            <!-- 👤 Account Manager -->
            <td data-label="Account Manager"><%= org.account_manager || 'N/A' %></td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  </div>
</div>

<%- include('partials/footer') %>
