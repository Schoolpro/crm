<% if (user) { %>
  <div class="text-end p-2 bg-light text-dark">
    👋 Bienvenido, <%= user.email %> (<%= user.role %>) |
    <a href="/logout">Cerrar sesión</a>
  </div>
<% } else { %>
  <div class="text-end p-2 bg-light text-dark">
    <a href="/login">Iniciar sesión</a>
  </div>
<% } %>


<% if (user) { %>
  <!-- NAVBAR FIJA Y FULL-WIDTH -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark sticky-top shadow-sm">
    <div class="container-fluid">
      <!-- LOGO / TITULO -->
      <a class="navbar-brand fw-bold" href="/">Mi CRM</a>

      <!-- BOTÓN MENÚ HAMBURGUESA -->
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent">
        <span class="navbar-toggler-icon"></span>
      </button>

      <!-- CONTENIDO DEL NAVBAR -->
      <div class="collapse navbar-collapse justify-content-between" id="navbarContent">
        <!-- 🔍 BUSCADOR CENTRADO -->
        <form class="d-flex mx-auto" role="search" style="max-width: 500px; width: 100%;">
          <input
            class="form-control me-2 bg-light border-0 rounded-pill px-3 shadow-sm"
            type="search"
            placeholder="Buscar contactos u organizaciones"
            aria-label="Buscar"
          />
          <button class="btn btn-outline-light rounded-pill px-4" type="submit">🔍</button>
        </form>

        <!-- MENÚ A LA DERECHA -->
        <ul class="navbar-nav ms-auto">
          <li class="nav-item">
            <a class="nav-link" href="/organizations">Organizaciones</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/contacts">Contactos</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/todo">To-do</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/">Inicio</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>
<% } %>
